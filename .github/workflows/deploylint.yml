# Generated by DeployLint generator v5. Plan 624005f2d0ab6b53933a7a297f077fd1e09810e4748616ef88952e4ff491739c.
name: DeployLint CI/CD
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
  push: {}
permissions:
  contents: read
jobs:
  dependency-review:
    name: Review dependency changes
    if: github.event_name == 'pull_request' && (github.event.repository.private == false || vars.DEPENDENCY_REVIEW_ENABLED == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - &a1
        name: Checkout immutable source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - name: Review dependency changes
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48
  ci:
    name: CI checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - *a1
      - name: Set up pinned Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install locked dependencies
        run: npm ci
      - name: Run test
        run: npm run test
      - name: Run build
        run: npm run build
  deployment-dry-run:
    name: Cloudflare deployment dry run
    if: github.event_name == 'pull_request'
    needs:
      - ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - *a1
      - name: Set up pinned Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install locked dependencies
        run: npm ci
      - &a2
        name: Verify locked Cloudflare deployment tool
        shell: bash
        run: |-
          if [ ! -x ./node_modules/.bin/wrangler ]; then
            echo "::error title=Missing locked Cloudflare deployment tool::Add wrangler to devDependencies and commit the updated lockfile."
            exit 1
          fi
      - name: Validate Cloudflare deployment
        run: ./node_modules/.bin/wrangler deploy --dry-run --config wrangler.jsonc
  deploy:
    name: Deploy production
    if: github.event_name == 'push' && github.ref_name == github.event.repository.default_branch
    needs:
      - ci
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    concurrency:
      group: deploylint-production
      cancel-in-progress: false
    steps:
      - *a1
      - name: Set up pinned Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Install locked dependencies
        run: npm ci
      - *a2
      - name: Verify required Cloudflare credentials
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |-
          missing=0
          if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "::error title=Missing Cloudflare credential::Add CLOUDFLARE_API_TOKEN to the protected production environment."
            missing=1
          fi
          if [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "::error title=Missing Cloudflare credential::Add CLOUDFLARE_ACCOUNT_ID to the protected production environment."
            missing=1
          fi
          test "$missing" -eq 0
      - name: Deploy Cloudflare Worker
        run: ./node_modules/.bin/wrangler deploy --config wrangler.jsonc
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
